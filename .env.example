# nanoquant 环境变量模板
# 复制本文件为 .env 并填入真实值；.env 不进 git。

# ===== Tushare =====
# 在 https://tushare.pro 注册后获取 token
TUSHARE_TOKEN=

# ===== MySQL =====
# 库名固定 stock（见 config/database.py）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_DATABASE=stock

# ===== 可选：调度/日志 =====
# LOG_DIR=logs
